'use client'; import React from 'react'; import { motion, AnimatePresence } from 'framer-motion'; import { Zap, ArrowLeft, ArrowRight, CheckCircle2, XCircle, Trophy, Timer, RefreshCw, AlertCircle, Lightbulb, Calendar, Clock } from 'lucide-react'; import { gkSupabase } from '@/lib/gk-supabase'; interface WeeklyQuestion { id: number; week_number: number; question: string; options: string[]; correct: number; explanation: string; topic?: string; } interface GKWeeklyTestProps { onComplete?: (results: { score: number; total: number; timeSpent: number }) => void; onBack?: () => void; } const WEEK_LABELS = [ { week: 1, label: 'Week 1', desc: 'Jan 2026 Wk 1' }, { week: 2, label: 'Week 2', desc: 'Jan 2026 Wk 2' }, { week: 3, label: 'Week 3', desc: 'Jan 2026 Wk 3' }, { week: 4, label: 'Week 4', desc: 'Jan 2026 Wk 4' }, { week: 5, label: 'Week 5', desc: 'Feb 2026 Wk 1' }, { week: 6, label: 'Week 6', desc: 'Feb 2026 Wk 2' }, { week: 7, label: 'Week 7', desc: 'Feb 2026 Wk 3' }, { week: 8, label: 'Week 8', desc: 'Feb 2026 Wk 4' }, { week: 9, label: 'Week 9', desc: 'Mar 2026 Wk 1' }, { week: 10, label: 'Week 10', desc: 'Mar 2026 Wk 2' }, { week: 11, label: 'Week 11', desc: 'Mar 2026 Wk 3' }, { week: 12, label: 'Week 12', desc: 'Mar 2026 Wk 4' }, ]; function WeekSelector({ onSelect, onBack }: { onSelect: (week: number) => void; onBack?: () => void }) { return ( <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="space-y-8" > <div className="flex items-center justify-between"> {onBack && ( <button onClick={onBack} className="flex items-center gap-2 text-gray-500 hover:text-[#F59E0B] transition-colors font-bold uppercase text-[10px] tracking-widest" > <ArrowLeft size={16} /> Back to Engine </button> )} <div className={onBack ? '' : 'w-full'}> <h2 className="text-3xl font-black text-[#060818] dark:text-white tracking-tight"> Weekly Quizzes </h2> <p className="text-gray-500 dark:text-gray-400 font-medium text-sm mt-1"> Fresh current affairs questions — one quiz per week. </p> </div> </div> <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4"> {WEEK_LABELS.map((w, idx) => ( <motion.button key={w.week} initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: idx * 0.04 }} whileHover={{ y: -4, scale: 1.02 }} whileTap={{ scale: 0.97 }} onClick={() => onSelect(w.week)} className="group bg-white dark:bg-white/5 p-6 rounded-3xl border border-gray-100 dark:border-white/10 text-left hover:shadow-xl hover:shadow-[#F59E0B]/10 hover:border-[#F59E0B]/30 transition-all duration-300 space-y-4" > <div className="w-12 h-12 bg-[#F59E0B]/10 rounded-2xl flex items-center justify-center group-hover:bg-[#F59E0B] transition-colors"> <Calendar size={22} className="text-[#F59E0B] group-hover:text-[#060818] transition-colors" /> </div> <div> <p className="font-black text-[#060818] dark:text-white text-base group-hover:text-[#F59E0B] transition-colors"> {w.label} </p> <p className="text-[10px] font-bold text-gray-400 uppercase tracking-widest mt-0.5"> {w.desc} </p> </div> <div className="flex items-center justify-between pt-2 border-t border-gray-50 dark:border-white/5"> <span className="text-[10px] font-black text-gray-400 uppercase tracking-widest">20 Qs</span> <ArrowRight size={14} className="text-gray-300 group-hover:text-[#F59E0B] group-hover:translate-x-1 transition-all" /> </div> </motion.button> ))} </div> </motion.div> ); } function QuizScreen({ questions, weekNum, onComplete, onBack, }: { questions: WeeklyQuestion[]; weekNum: number; onComplete: (r: { score: number; total: number; timeSpent: number }) => void; onBack: () => void; }) { const [current, setCurrent] = React.useState(0); const [answers, setAnswers] = React.useState<(number | null)[]>(Array(questions.length).fill(null)); const [answered, setAnswered] = React.useState(false); const [selected, setSelected] = React.useState<number | null>(null); const [timeLeft, setTimeLeft] = React.useState(questions.length * 60); const [startTime] = React.useState(Date.now()); const [showResults, setShowResults] = React.useState(false); React.useEffect(() => { if (showResults) return; const interval = setInterval(() => { setTimeLeft(prev => { if (prev <= 1) { clearInterval(interval); handleFinish(answers); return 0; } return prev - 1; }); }, 1000); return () => clearInterval(interval); }, [showResults]); const handleAnswer = (optIndex: number) => { if (answered) return; setSelected(optIndex); setAnswered(true); const updated = [...answers]; updated[current] = optIndex; setAnswers(updated); }; const handleFinish = (finalAnswers: (number | null)[]) => { const timeSpent = Math.round((Date.now() - startTime) / 1000); const score = finalAnswers.reduce((acc, ans, i) => { return ans === questions[i].correct ? acc + 1 : acc; }, 0) as number; // Write analytics to localStorage const pct = Math.round((score / questions.length) * 100); const existing = parseInt(localStorage.getItem('gk_quizzesTaken') || '0', 10); localStorage.setItem('gk_quizzesTaken', String(existing + 1)); const prevAcc = parseInt(localStorage.getItem('gk_accuracy') || '0', 10); const newAcc = prevAcc > 0 ? Math.round((prevAcc + pct) / 2) : pct; localStorage.setItem('gk_accuracy', String(newAcc)); onComplete({ score, total: questions.length, timeSpent }); }; const handleNext = () => { if (current < questions.length - 1) { setCurrent(current + 1); setAnswered(answers[current + 1] !== null); setSelected(answers[current + 1]); } else { handleFinish(answers); } }; const q = questions[current]; const mins = Math.floor(timeLeft / 60); const secs = timeLeft % 60; const progress = ((current + 1) / questions.length) * 100; const isCorrect = selected === q.correct; const OPTION_LABELS = ['A', 'B', 'C', 'D']; return ( <motion.div key={current} initial={{ opacity: 0, x: 30 }} animate={{ opacity: 1, x: 0 }} className="space-y-6 max-w-2xl mx-auto" > {/* Header */} <div className="flex items-center justify-between"> <button onClick={onBack} className="flex items-center gap-2 text-gray-500 hover:text-[#F59E0B] transition-colors font-bold uppercase text-[10px] tracking-widest" > <ArrowLeft size={14} /> Week {weekNum} </button> <div className={`flex items-center gap-2 px-4 py-2 rounded-2xl font-black text-sm ${ timeLeft < 60 ? 'bg-red-500/10 text-red-500' : 'bg-[#F59E0B]/10 text-[#F59E0B]' }`}> <Timer size={16} /> {mins}:{secs.toString().padStart(2, '0')} </div> </div> {/* Progress Bar */} <div className="space-y-2"> <div className="flex justify-between"> <span className="text-[10px] font-black text-gray-400 uppercase tracking-widest"> Question {current + 1} of {questions.length} </span> <span className="text-[10px] font-black text-[#F59E0B] uppercase tracking-widest"> Week {weekNum} </span> </div> <div className="h-1.5 bg-gray-100 dark:bg-white/10 rounded-full overflow-hidden"> <motion.div className="h-full bg-[#F59E0B] rounded-full" initial={{ width: 0 }} animate={{ width: `${progress}%` }} transition={{ duration: 0.4 }} /> </div> </div> {/* Question */} <AnimatePresence mode="wait"> <motion.div key={current} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -10 }} className="bg-white dark:bg-white/5 p-8 rounded-[2.5rem] border border-gray-100 dark:border-white/10 space-y-6" > {q.topic && ( <span className="inline-block text-[10px] font-black uppercase tracking-widest text-[#F59E0B] bg-[#F59E0B]/10 px-3 py-1 rounded-full"> {q.topic} </span> )} <p className="text-base font-bold text-[#060818] dark:text-white leading-relaxed"> {q.question} </p> <div className="space-y-3"> {q.options.map((opt, i) => { let style = 'bg-gray-50 dark:bg-white/5 border-gray-100 dark:border-white/10 text-gray-700 dark:text-gray-300 hover:border-[#F59E0B]/40'; if (answered) { if (i === q.correct) style = 'bg-green-50 dark:bg-green-500/10 border-green-400 text-green-700 dark:text-green-300'; else if (i === selected && selected !== q.correct) style = 'bg-red-50 dark:bg-red-500/10 border-red-400 text-red-700 dark:text-red-300'; else style = 'bg-gray-50 dark:bg-white/5 border-gray-100 dark:border-white/10 text-gray-400 opacity-60'; } return ( <motion.button key={i} whileTap={!answered ? { scale: 0.98 } : {}} onClick={() => handleAnswer(i)} disabled={answered} className={`w-full flex items-center gap-4 p-4 rounded-2xl border-2 transition-all text-left font-medium text-sm ${style} ${!answered ? 'cursor-pointer' : 'cursor-default'}`} > <span className={`w-7 h-7 rounded-xl flex items-center justify-center text-[11px] font-black shrink-0 ${ answered && i === q.correct ? 'bg-green-500 text-white' : answered && i === selected && selected !== q.correct ? 'bg-red-500 text-white' : 'bg-gray-200 dark:bg-white/10 text-gray-500' }`}> {answered && i === q.correct ? <CheckCircle2 size={14} /> : answered && i === selected && selected !== q.correct ? <XCircle size={14} /> : OPTION_LABELS[i]} </span> {opt} </motion.button> ); })} </div> <AnimatePresence> {answered && ( <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className={`p-4 rounded-2xl border ${ isCorrect ? 'bg-green-50 dark:bg-green-500/10 border-green-200 dark:border-green-500/20' : 'bg-amber-50 dark:bg-[#F59E0B]/10 border-amber-200 dark:border-[#F59E0B]/20' }`} > <div className="flex items-center gap-2 mb-2"> {isCorrect ? <CheckCircle2 size={14} className="text-green-600" /> : <Lightbulb size={14} className="text-[#F59E0B]" /> } <span className={`text-[10px] font-black uppercase tracking-widest ${isCorrect ? 'text-green-700' : 'text-[#F59E0B]'}`}> {isCorrect ? 'Correct!' : 'Explanation'} </span> </div> <p className="text-xs font-medium leading-relaxed text-gray-700 dark:text-gray-300"> {q.explanation} </p> </motion.div> )} </AnimatePresence> </motion.div> </AnimatePresence> {answered && ( <motion.button initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} onClick={handleNext} className="w-full py-4 bg-[#060818] dark:bg-[#F59E0B] text-white dark:text-[#060818] rounded-2xl font-black text-sm flex items-center justify-center gap-2 hover:opacity-90 transition-all shadow-xl" > {current < questions.length - 1 ? ( <><ArrowRight size={18} /> Next Question</> ) : ( <><Trophy size={18} /> View Results</> )} </motion.button> )} {/* Nav dots */} <div className="flex flex-wrap gap-1.5 justify-center"> {questions.map((_, i) => ( <div key={i} className={`h-2 rounded-full transition-all ${ i === current ? 'w-4 bg-[#F59E0B]' : answers[i] !== null ? answers[i] === questions[i].correct ? 'w-2 bg-green-400' : 'w-2 bg-red-400' : 'w-2 bg-gray-200 dark:bg-white/20' }`} /> ))} </div> </motion.div> ); } export default function GKWeeklyTest({ onComplete, onBack }: GKWeeklyTestProps) { const [selectedWeek, setSelectedWeek] = React.useState<number | null>(null); const [questions, setQuestions] = React.useState<WeeklyQuestion[]>([]); const [loading, setLoading] = React.useState(false); const [error, setError] = React.useState<string | null>(null); const [comingSoon, setComingSoon] = React.useState(false); const loadWeek = async (weekNum: number) => { setLoading(true); setError(null); setComingSoon(false); try { const { data, error: err } = await gkSupabase .from('gk_weekly_questions') .select('*') .eq('week_number', weekNum) .order('id'); if (err) { // Table doesn't exist yet — show Coming Soon state if (err.code === 'PGRST205' || err.message?.includes('schema cache') || err.message?.includes('does not exist')) { setComingSoon(true); return; } throw err; } if (!data || data.length === 0) { setComingSoon(true); return; } setQuestions(data as WeeklyQuestion[]); setSelectedWeek(weekNum); } catch (e: unknown) { const message = e instanceof Error ? e.message : 'Failed to load questions.'; setError(message); } finally { setLoading(false); } }; if (loading) { return ( <div className="flex flex-col items-center justify-center py-20 gap-4"> <motion.div animate={{ rotate: 360 }} transition={{ repeat: Infinity, duration: 1, ease: 'linear' }} > <RefreshCw size={32} className="text-[#F59E0B]" /> </motion.div> <p className="text-sm font-bold text-gray-500 dark:text-gray-400">Loading questions…</p> </div> ); } if (comingSoon) { return ( <motion.div initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} className="flex flex-col items-center justify-center py-20 gap-6 text-center" > <div className="w-20 h-20 bg-[#F59E0B]/10 rounded-full flex items-center justify-center"> <Clock size={40} className="text-[#F59E0B]" /> </div> <div className="space-y-2"> <h3 className="text-2xl font-black text-[#060818] dark:text-white">Questions Coming Soon!</h3> <p className="text-sm text-gray-500 dark:text-gray-400 font-medium max-w-xs"> Weekly quiz questions are being prepared by our GK team and will be available shortly. Check back later! </p> </div> <button onClick={() => { setComingSoon(false); setSelectedWeek(null); }} className="px-6 py-3 bg-[#F59E0B] text-[#060818] rounded-2xl font-black text-sm hover:shadow-lg hover:shadow-[#F59E0B]/20 transition-all" > ← Choose Another Week </button> </motion.div> ); } if (error) { return ( <div className="flex flex-col items-center justify-center py-20 gap-4 text-center"> <AlertCircle size={40} className="text-red-500" /> <p className="font-bold text-[#060818] dark:text-white">Failed to load questions</p> <p className="text-sm text-gray-500">{error}</p> <button onClick={() => { setError(null); setSelectedWeek(null); }} className="px-5 py-2 bg-[#F59E0B] text-[#060818] rounded-xl font-black text-sm" > Try Again </button> </div> ); } if (!selectedWeek) { return <WeekSelector onSelect={loadWeek} onBack={onBack} />; } return ( <QuizScreen questions={questions} weekNum={selectedWeek} onComplete={onComplete || (() => {})} onBack={() => setSelectedWeek(null)} /> ); }
