'use client'; import React from 'react'; import { motion, AnimatePresence } from 'framer-motion'; import { User, ChevronDown, LayoutDashboard, FileText, Zap, Brain, BookMarked, CalendarDays, Search, X } from 'lucide-react'; import { useRouter } from 'next/navigation'; import { gkSupabase } from '@/lib/gk-supabase'; interface GKLayoutProps { children: React.ReactNode; activeTab: string; setActiveTab: (tab: string) => void; userName?: string; userEmail?: string; } interface SearchResult { id: string; title: string; type: 'editorial' | 'vault' | 'flashcard'; tab: string; } export default function GKLayout({ children, activeTab, setActiveTab, userName = 'Student', userEmail = '' }: GKLayoutProps) { const router = useRouter(); const [isMobileMenuOpen, setIsMobileMenuOpen] = React.useState(false); const [isProfileOpen, setIsProfileOpen] = React.useState(false); const [theme, setTheme] = React.useState<'light' | 'dark'>('dark'); const [toast, setToast] = React.useState<string | null>(null); const [mounted, setMounted] = React.useState(false); const [searchQuery, setSearchQuery] = React.useState(''); const [searchResults, setSearchResults] = React.useState<SearchResult[]>([]); const [isSearching, setIsSearching] = React.useState(false); const [showSearch, setShowSearch] = React.useState(false); const searchRef = React.useRef<HTMLDivElement>(null); const initials = userName .split(' ') .filter(Boolean) .map((w) => w[0].toUpperCase()) .join('') .substring(0, 2) || 'ST'; const firstName = userName.split(' ')[0] || 'Student'; React.useEffect(() => { setMounted(true); const saved = localStorage.getItem('gk_theme') as 'light' | 'dark' | null; if (saved) setTheme(saved); }, []); React.useEffect(() => { if (!mounted) return; const root = window.document.documentElement; if (theme === 'dark') { root.classList.add('dark'); } else { root.classList.remove('dark'); } localStorage.setItem('gk_theme', theme); }, [theme, mounted]); // Close search on outside click React.useEffect(() => { const handleClick = (e: MouseEvent) => { if (searchRef.current && !searchRef.current.contains(e.target as Node)) { setShowSearch(false); } }; document.addEventListener('mousedown', handleClick); return () => document.removeEventListener('mousedown', handleClick); }, []); // Debounced search across Supabase React.useEffect(() => { if (!searchQuery.trim() || searchQuery.length < 2) { setSearchResults([]); setIsSearching(false); return; } setIsSearching(true); const timer = setTimeout(async () => { try { const SUPABASE_URL = 'https://fjswchcothephgtzqbgq.supabase.co'; const SUPABASE_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.' + 'eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZqc3djaGNvdGhlcGhndHpxYmdxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTU3NTk2ODQsImV4cCI6MjA3MTMzNTY4NH0.' + 'AqKfl8rwkH4_Y0sVdcQLWu6HF1nrxhro-jMyEdUggV4'; const results: SearchResult[] = []; // Search editorials const editResp = await fetch( `${SUPABASE_URL}/rest/v1/gk_editorials?title=ilike.*${encodeURIComponent(searchQuery)}*&select=id,title&limit=5`, { headers: { apikey: SUPABASE_KEY, Authorization: `Bearer ${SUPABASE_KEY}` } } ); if (editResp.ok) { const editRows: any[] = await editResp.json(); editRows.forEach(r => results.push({ id: r.id, title: r.title, type: 'editorial', tab: 'editorial' })); } // Search flashcards const flashResp = await fetch( `${SUPABASE_URL}/rest/v1/flashcard?front=ilike.*${encodeURIComponent(searchQuery)}*&select=id,front&limit=5`, { headers: { apikey: SUPABASE_KEY, Authorization: `Bearer ${SUPABASE_KEY}` } } ); if (flashResp.ok) { const flashRows: any[] = await flashResp.json(); flashRows.forEach(r => results.push({ id: r.id, title: r.front, type: 'flashcard', tab: 'flashcards' })); } setSearchResults(results); } catch { setSearchResults([]); } finally { setIsSearching(false); } }, 350); return () => clearTimeout(timer); }, [searchQuery]); const showToast = (msg: string) => { setToast(msg); setTimeout(() => setToast(null), 3000); }; const toggleTheme = () => { const newTheme = theme === 'light' ? 'dark' : 'light'; setTheme(newTheme); showToast(`Switched to ${newTheme} mode`); }; const handleSignOut = async () => { setIsProfileOpen(false); showToast('Signing out…'); await gkSupabase.auth.signOut(); localStorage.removeItem('gk_userName'); localStorage.removeItem('gk_userEmail'); localStorage.removeItem('gk_isSubscribed'); localStorage.removeItem('gk_registrationDate'); setTimeout(() => router.replace('/gk/login'), 800); }; const handleSearchSelect = (result: SearchResult) => { setActiveTab(result.tab); // Store pending search target so the tab can deep-link if needed localStorage.setItem('gk_pendingSearch', JSON.stringify({ id: result.id, title: result.title, type: result.type })); setSearchQuery(''); setShowSearch(false); }; const navLinks = [ { id: 'dashboard', name: 'Dashboard', icon: LayoutDashboard, badge: null }, { id: 'editorial', name: 'Editorials', icon: FileText, badge: 'NEW' }, { id: 'flashcards', name: 'Flashcards', icon: Zap, badge: null }, { id: 'testing', name: 'Testing Engine', icon: Brain, badge: 'NEW' }, { id: 'vault', name: 'GK Vault', icon: BookMarked, badge: null }, { id: 'monthly-summary', name: 'Monthly Summary', icon: CalendarDays, badge: null }, ]; return ( <div className={`flex flex-col gk-portal${theme === 'dark' ? ' dark' : ''} min-h-screen bg-white dark:bg-[#060818] font-sans text-[#060818] dark:text-white transition-colors duration-300 relative`}> {toast && ( <motion.div initial={{ opacity: 0, y: 50 }} animate={{ opacity: 1, y: 0 }} className="fixed bottom-6 left-4 right-4 sm:left-1/2 sm:right-auto sm:-translate-x-1/2 sm:w-auto z-[100] bg-[#060818] dark:bg-[#F59E0B] text-white dark:text-[#060818] px-6 py-3 rounded-2xl font-bold shadow-2xl border border-white/10 text-center" > {toast} </motion.div> )} {/* clattribe.com header */} <header className="sticky top-0 z-[60] bg-[#060818]/80 backdrop-blur-md border-b border-white/[0.08] shadow-[0_1px_24px_rgba(0,0,0,0.45)]"> <div className="mx-auto px-4 py-3 flex items-center justify-between"> <a href="https://www.clattribe.com" target="_blank" rel="noopener noreferrer"> <img src="https://www.clattribe.com/heading.png" alt="CLAT Tribe" className="h-9 w-auto" /> </a> <div className="hidden md:flex items-center gap-5 text-[13px] text-white/75"> <a href="https://www.clattribe.com" className="hover:text-white transition-colors">Home</a> <div className="relative group"> <button className="flex items-center gap-1 hover:text-white transition-colors bg-transparent border-0 cursor-pointer text-[13px] text-white/75"> About Us <ChevronDown className="w-3 h-3" /> </button> <div className="absolute top-full left-0 mt-2 w-44 bg-[#0d1235] border border-white/10 rounded-lg shadow-xl py-1 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-150 z-50"> <a href="https://www.clattribe.com/our-team" className="block px-4 py-2 text-white/70 hover:text-white hover:bg-white/5 transition-colors">Our Team</a> <a href="https://www.clattribe.com/our-courses" className="block px-4 py-2 text-white/70 hover:text-white hover:bg-white/5 transition-colors">Our Courses</a> </div> </div> <a href="https://www.clattribe.com" className="hover:text-white transition-colors">DSB Challenge</a> <a href="https://www.clattribe.com/clat-2027-starter-kit" className="hover:text-white transition-colors">CLAT 2027 Starter Kit</a> <button onClick={() => setActiveTab('flashcards')} className="hover:text-white transition-colors bg-transparent border-0 cursor-pointer text-[13px] text-white/75">Flashcards</button> <a href="https://www.clattribe.com/blogs" className="hover:text-white transition-colors">Blogs</a> <a href="https://www.clattribe.com/clat-gk-vault" className="hover:text-white transition-colors">CLAT GK Vault</a> </div> </div> </header> <div className="flex flex-1 overflow-hidden"> <nav className="bg-white dark:bg-[#060818] border-r border-gray-100 dark:border-white/5 h-full flex flex-col overflow-y-auto w-56 shrink-0 transition-colors"> {/* Search bar */} <div className="px-3 pt-4 pb-2" ref={searchRef}> <div className="relative"> <div className="flex items-center gap-2 px-3 py-2 rounded-xl bg-gray-50 dark:bg-white/5 border border-gray-100 dark:border-white/10 focus-within:border-[#F59E0B]/50 transition-colors"> <Search size={14} className="text-gray-400 shrink-0" /> <input type="text" value={searchQuery} onChange={(e) => { setSearchQuery(e.target.value); setShowSearch(true); }} onFocus={() => setShowSearch(true)} placeholder="Search GK…" className="bg-transparent text-xs font-medium text-gray-700 dark:text-gray-300 placeholder:text-gray-400 outline-none flex-1 min-w-0" /> {searchQuery && ( <button onClick={() => { setSearchQuery(''); setSearchResults([]); }} className="text-gray-400 hover:text-gray-600 transition-colors" > <X size={12} /> </button> )} </div> {/* Search dropdown */} <AnimatePresence> {showSearch && (searchQuery.length >= 2) && ( <motion.div initial={{ opacity: 0, y: -4 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -4 }} className="absolute top-full left-0 right-0 mt-1 bg-white dark:bg-[#0d1235] border border-gray-100 dark:border-white/10 rounded-xl shadow-2xl z-50 overflow-hidden" > {isSearching ? ( <div className="px-4 py-3 text-xs text-gray-400 font-medium text-center">Searching…</div> ) : searchResults.length === 0 ? ( <div className="px-4 py-3 text-xs text-gray-400 font-medium text-center">No results found</div> ) : ( <div className="py-1"> {searchResults.map((r) => ( <button key={`${r.type}-${r.id}`} onClick={() => handleSearchSelect(r)} className="w-full flex items-center gap-2.5 px-3 py-2.5 hover:bg-gray-50 dark:hover:bg-white/5 transition-colors text-left" > <div className={`w-6 h-6 rounded-lg flex items-center justify-center shrink-0 ${ r.type === 'editorial' ? 'bg-blue-100 text-blue-600' : r.type === 'flashcard' ? 'bg-amber-100 text-amber-600' : 'bg-green-100 text-green-600' }`}> {r.type === 'editorial' ? <FileText size={10} /> : r.type === 'flashcard' ? <Zap size={10} /> : <BookMarked size={10} />} </div> <div className="flex-1 min-w-0"> <p className="text-xs font-semibold text-gray-800 dark:text-white truncate">{r.title}</p> <p className="text-[10px] text-gray-400 uppercase tracking-widest">{r.type}</p> </div> </button> ))} </div> )} </motion.div> )} </AnimatePresence> </div> </div> {/* Nav links */} <div className="flex flex-col gap-0.5 px-3 flex-1 pt-2 pb-4"> {navLinks.map((link) => { const Icon = link.icon; const isActive = activeTab === link.id; return ( <div key={link.id} onClick={() => setActiveTab(link.id)} className={`flex items-center gap-3 px-3 py-2.5 rounded-xl cursor-pointer transition-all duration-150 ${ isActive ? 'bg-amber-50 dark:bg-amber-500/10 text-amber-500' : 'text-gray-500 dark:text-gray-400 hover:bg-gray-50 dark:hover:bg-white/5 hover:text-gray-800 dark:hover:text-white' }`} > <Icon size={18} className="shrink-0" /> <span className="text-sm font-medium leading-none flex-1">{link.name}</span> {link.badge && !isActive && ( <span className="text-[8px] font-black bg-[#F59E0B] text-[#060818] px-1.5 py-0.5 rounded-full uppercase tracking-wide shrink-0"> {link.badge} </span> )} {isActive && <div className="ml-auto w-1.5 h-1.5 rounded-full bg-amber-400 shrink-0" />} </div> ); })} </div> {/* Profile footer */} <div className="px-3 pb-4 pt-2 border-t border-gray-100 dark:border-white/10"> <div onClick={() => setActiveTab('profile')} className={`flex items-center gap-2.5 px-3 py-2.5 rounded-xl cursor-pointer transition-all duration-150 ${ activeTab === 'profile' ? 'bg-amber-50 text-amber-500' : 'text-gray-500 hover:bg-gray-50 hover:text-gray-800' }`} > <div className="w-7 h-7 rounded-full bg-gray-100 dark:bg-white/10 flex items-center justify-center shrink-0"> <User size={13} /> </div> <div className="flex flex-col min-w-0 flex-1 leading-tight"> <span className="text-xs font-semibold text-gray-800 dark:text-white truncate">{userName}</span> <span className="text-[10px] text-gray-400 truncate">{userEmail}</span> </div> </div> </div> </nav> <main className="flex-1 overflow-auto max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 sm:py-6 lg:py-8">{children}</main> </div> </div> ); }
